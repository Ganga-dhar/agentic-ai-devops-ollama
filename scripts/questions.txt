# Agent validation questions for CI — full ReAct loop (LLM + tools)
# Format: CATEGORY | question
# The agent decides which tool to call (kubectl / docker) and when.
# Categories: docker, kubernetes, reasoning

docker     | How many Docker containers are currently running? Use the docker tool to check.
docker     | List all Docker images on this machine with their sizes. Use the docker tool.
kubernetes | Run kubectl version --client and report what you find.
reasoning  | In one paragraph, what is the difference between a Docker container and a Kubernetes Pod?
